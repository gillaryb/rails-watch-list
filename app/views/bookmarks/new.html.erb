<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit class: 'btn btn-primary' %>
  <% end %>
  <%#= link_to 'Delete', list_path(list), data: { turbo_method: :delete, turbo_confirm: 'Delete bookmark?' }  %>
</div>
