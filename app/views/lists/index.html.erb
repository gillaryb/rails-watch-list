<div class="container">
  <h1> Movie Watchlist </h1>
  <ul>
    <% @lists.each do |list| %>
      <li> <%= link_to list.name, list_path(list), class: 'text-dark text-decoration-none'%></li>
    <% end %>
  </ul>
  <%= link_to 'Add a list', new_list_path, class: 'btn btn-primary'  %>
</div>
